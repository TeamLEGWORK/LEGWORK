# NOTE: any changes in the requirements must also be reflected in
#   - setup.cfg
#   - environment.yml
#   - install.rst
numba >= '0.50'
numpy >= '1.17'
astropy >= '4.0'
scipy >= '1.5.0'
matplotlib >= '3.3.2'
seaborn >= '0.11.1'
schwimmbad >= '0.3.2'
Jinja2 == '2.11'
